//Erstelle eine let-Variable namens "city" und weise ihr den Wert "Berlin" zu.

// Gib den Wert der Variable "city" in der Konsole aus.

// Ändere den Wert der Variable "city" auf "München".

// Gib den neuen Wert der Variable "city" in der Konsole aus.


// erstelle eine const-Variable namens "country" und weise ihr den Wert "Deutschland" zu.

// Gib den Wert der Variable "country" in der Konsole aus.

// Versuche, den Wert der Variable "country" auf "Österreich" zu ändern (dies sollte einen Fehler verursachen).

// Gib den Wert der Variable "country" in der Konsole aus, um zu bestätigen, dass er sich nicht geändert hat.