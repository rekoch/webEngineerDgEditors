/**
 * Übungen zu Objekten mit Funktionen
 * Erstelle ein Objekt CoffeeMachine mit folgenden Eigenschaften und Methoden:
 * - Eigenschaften:
 *   - brand (String)
 *   - waterLevel (Number, initial 0)
 * - Methoden:
 *   - fillWater(amount): Erhöht den waterLevel um den angegebenen Betrag.
 *   - makeCoffee(): Gibt eine Nachricht in der Konsole aus, dass Kaffee gemacht wird,
 *     und verringert den waterLevel um 1. Wenn nicht genug Wasser vorhanden ist,
 *     soll eine Warnung ausgegeben werden.
 * 
 * Teste dein CoffeeMachine-Objekt, indem du Wasser hinzufügst und Kaffee machst.
 */